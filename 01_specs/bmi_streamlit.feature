@level=L1-P
@module=bmi_streamlit
@mutation_threshold=90

Feature: BMI 計算器 Streamlit Web App
  As a 使用者
  I want 在網頁上輸入體重和身高並點擊計算
  So that 能即時看到 BMI 數值和 WHO 健康分類

  Background:
    Given Streamlit app 已啟動並完成初始渲染

  # ────────────────────────────────────────
  # 正常輸入 + 四分類各一例
  # ────────────────────────────────────────

  Scenario: 正常輸入顯示 BMI 22.86 和「正常」分類
    Given 使用者在體重欄輸入 "70"
    And 使用者在身高欄輸入 "1.75"
    When 使用者點擊「計算 BMI」按鈕
    Then 頁面顯示 BMI 指數為 "22.86"
    And 頁面顯示健康分類為 "正常"
    And 頁面不顯示任何錯誤訊息

  Scenario: 偏瘦輸入顯示「偏瘦」分類
    Given 使用者在體重欄輸入 "45"
    And 使用者在身高欄輸入 "1.70"
    When 使用者點擊「計算 BMI」按鈕
    Then 頁面顯示 BMI 指數為 "15.57"
    And 頁面顯示健康分類為 "偏瘦"
    And 頁面不顯示任何錯誤訊息

  Scenario: 過重輸入顯示「過重」分類
    Given 使用者在體重欄輸入 "80"
    And 使用者在身高欄輸入 "1.70"
    When 使用者點擊「計算 BMI」按鈕
    Then 頁面顯示 BMI 指數為 "27.68"
    And 頁面顯示健康分類為 "過重"
    And 頁面不顯示任何錯誤訊息

  Scenario: 肥胖輸入顯示「肥胖」分類
    Given 使用者在體重欄輸入 "100"
    And 使用者在身高欄輸入 "1.70"
    When 使用者點擊「計算 BMI」按鈕
    Then 頁面顯示 BMI 指數為 "34.60"
    And 頁面顯示健康分類為 "肥胖"
    And 頁面不顯示任何錯誤訊息

  # ────────────────────────────────────────
  # 邊界值
  # ────────────────────────────────────────

  Scenario: 邊界值 BMI=18.50 歸入「正常」
    # 47.36 / (1.60 ** 2) = 47.36 / 2.56 = 18.5 → round(18.5, 2) = 18.5
    # 驗收重點：≥18.5 應歸「正常」，不得歸「偏瘦」
    Given 使用者在體重欄輸入 "47.36"
    And 使用者在身高欄輸入 "1.60"
    When 使用者點擊「計算 BMI」按鈕
    Then 頁面顯示 BMI 指數為 "18.50"
    And 頁面顯示健康分類為 "正常"

  Scenario: 邊界值 BMI=25.00 歸入「過重」
    # 72.25 / (1.70 ** 2) = 72.25 / 2.89 = 25.0 → round(25.0, 2) = 25.0
    # 驗收重點：≥25.0 應歸「過重」，不得歸「正常」
    Given 使用者在體重欄輸入 "72.25"
    And 使用者在身高欄輸入 "1.70"
    When 使用者點擊「計算 BMI」按鈕
    Then 頁面顯示 BMI 指數為 "25.00"
    And 頁面顯示健康分類為 "過重"

  # ────────────────────────────────────────
  # 錯誤輸入
  # ────────────────────────────────────────

  Scenario: 負數體重輸入顯示錯誤、不顯示 BMI 數值
    Given 使用者在體重欄輸入 "-5"
    And 使用者在身高欄輸入 "1.70"
    When 使用者點擊「計算 BMI」按鈕
    Then 頁面顯示含有「體重」的錯誤訊息
    And 頁面不顯示任何 BMI 指數區塊
    And 頁面不顯示任何健康分類區塊

  Scenario: 零身高輸入顯示錯誤、不顯示 BMI 數值
    Given 使用者在體重欄輸入 "70"
    And 使用者在身高欄輸入 "0"
    When 使用者點擊「計算 BMI」按鈕
    Then 頁面顯示含有「身高」的錯誤訊息
    And 頁面不顯示任何 BMI 指數區塊
    And 頁面不顯示任何健康分類區塊
